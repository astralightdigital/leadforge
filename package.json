{
  "name": "leadforge",
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently --names \"server,client\" --prefix-colors \"cyan,teal\" \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "install:all": "npm install --prefix server && npm install --prefix client"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
